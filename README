This is the DEVELOPMENT build for GuildsSocial. The code in this build is unfinished and therefore unready for use by the general public. NEVER host this code present exclusively in this repository on a public URL. Code from the development build may be added to the production build after it is given a full pass for errors.
